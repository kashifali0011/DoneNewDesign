package com.dtech.done.doneapp.utilities.enums


enum class UserType(val value : Int){
    GUEST(0),CUSTOMER(1),PROVIDER(2)
}

enum class LocationLabel(val value : Int){
    HOME(0),WORK(1),OTHER(2)
}